#*****************************************************************
# Neo4j configuration
#*****************************************************************

# The name of the default database
dbms.default_database=neo4j

# Enable BOLT connector
dbms.connector.bolt.enabled=true
dbms.connector.bolt.listen_address=0.0.0.0:7687
dbms.connector.bolt.advertised_address=localhost:7687

# Enable HTTP connector
dbms.connector.http.enabled=true
dbms.connector.http.listen_address=0.0.0.0:7474
dbms.connector.http.advertised_address=localhost:7474

# Use temporary directory paths
dbms.directories.data=/tmp/neo4j_data/data
dbms.directories.plugins=/tmp/neo4j_data/plugins
dbms.directories.logs=/tmp/neo4j_data/logs
dbms.directories.import=/tmp/neo4j_data/import
dbms.directories.certificates=/tmp/neo4j_data/certificates
dbms.directories.lib=/tmp/neo4j_data/lib
dbms.directories.run=/tmp/neo4j_data/run
dbms.directories.metrics=/tmp/neo4j_data/metrics

# Memory settings - reduced for Replit environment
dbms.memory.heap.initial_size=256m
dbms.memory.heap.max_size=512m
dbms.memory.pagecache.size=256m
dbms.memory.off_heap.max_size=256m

# Security settings
dbms.security.auth_enabled=false
dbms.security.allow_csv_import_from_file_urls=true

# Default listen address
dbms.default_listen_address=0.0.0.0

# Performance tuning for Replit environment
dbms.tx_log.rotation.size=1M
dbms.memory.transaction.global_max_size=256m
dbms.jvm.additional=-XX:+UseG1GC
dbms.memory.pagecache.flush.enabled=false

# Logging configuration - simplified for Replit environment
dbms.logs.http.enabled=true
dbms.logs.debug.level=INFO

# User permissions
server.directories.mode=0777
server.logs.mode=0666